spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chatbot_db
    username: root
    password: 20031207

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration